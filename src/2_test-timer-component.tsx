import * as React from 'react';

const Timer = () => {
  return (
    <div>
      <h2>seconds: 0</h2>
      <button>Toggle</button>
      <button>Reset</button>
    </div>
  );
};

export default Timer;
